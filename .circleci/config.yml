version: 2.1

jobs:
  build:
    docker:
      - image: node:20-alpine3.16
    steps:
      - checkout
      - run: npm install
      - run: npm run build

workflows:
  build_and_publish:
    jobs:
      - build
